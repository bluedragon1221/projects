<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Favorite Articles</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@exampledev/new.css@1.1.3/new.min.css">
    <style>
        /* Custom styles for article cards */
        .article-card {
            background: var(--nc-bg-2);
            border: 1px solid var(--nc-bg-3);
            border-radius: 4px;
            padding: 1rem;
            margin-bottom: 1rem;
            transition: box-shadow 0.3s ease;
        }
        .article-card:hover {
            box-shadow: 0 2px 5px var(--nc-bg-3);
        }
        .article-card h2 {
            margin-top: 0;
            border-bottom: none;
            padding-bottom: 0.5rem;
        }
        .article-meta {
            font-size: 0.9rem;
            color: var(--nc-tx-2);
            margin-bottom: 0.5rem;
        }
        .article-summary {
            margin-bottom: 1rem;
        }
        .read-more {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: var(--nc-lk-1);
            color: var(--nc-lk-tx);
            text-decoration: none;
            border-radius: 4px;
            transition: background 0.3s ease;
        }
        .read-more:hover {
            background: var(--nc-lk-2);
        }
    </style>
</head>
<body>
    <header>
        <h1>My Favorite Articles</h1>
    </header>

    <main>
        {% if articles %}
            {% for frontmatter in articles %}
                <article class="article-card">
                    <h2>{{ frontmatter.title }}</h2>
                    <p class="article-meta">By {{ frontmatter.author|safe }}</p>
                    <p class="article-summary">{{ frontmatter.summary }}</p>
                    <a href="/{{ frontmatter.slug }}" class="read-more">Read more</a>
                </article>
            {% endfor %}
        {% else %}
            <p>No articles found.</p>
        {% endif %}
    </main>
</body>
</html>
